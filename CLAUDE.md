# CLAUDE.md

X4 Foundations (v9.0) の生産チェーン計算ツール。このファイルはリポジトリの前提・構造・編集規約を共有するためのコンテキストです。

## プロジェクト概要

目標とする生産ウェアと **モジュール数** を指定すると(毎時生産量はモジュール数 × outputRate で算出)、その素材調達に必要な **生産モジュール数** と、外部から調達する **採掘/購入材料の量** を再帰的に逆算する計算ツール。時間経過のシミュレーションは行わず、定常状態の必要量を静的に解く(= calculator / planner であって simulator ではない)。

- 単一の自己完結 HTML ファイル(`index.html`)のみ。ビルド・サーバ・バックエンドなし。
- ホスティング: GitHub Pages(Deploy from a branch / `main` / `(root)`)。
- 想定利用: PC でプレイしつつスマホのホーム画面アイコンから参照する二画面運用。

## 動作とデプロイ

- ローカル確認: `index.html` をブラウザで開くだけ。
- 本番: GitHub Pages。`index.html` をリポジトリ root に置き、Settings → Pages で `Deploy from a branch` → `main` / `(root)` を指定。公開 URL は `https://<user>.github.io/<repo>/` 形式。
- 更新: 新しい `index.html` を同リポジトリに上書きコミットすれば数分で反映。
- 外部依存: Google Fonts(Rajdhani / IBM Plex Mono)の CDN のみ。読み込めない場合はシステムフォントにフォールバックする。

## ファイル構成

- `index.html` — HTML / CSS / JS をすべてインラインで内包する 1 枚。これ以上ファイルを分割しないこと(後述「編集規約」参照)。

## アーキテクチャ / 主要ロジック

すべて `index.html` 内の `<script>` にある。

### データモデル(レシピ DB)
```
DB = {
  "<生産ウェア名>": {
    module: "<生産モジュール名>",   // 任意。省略時は表示で生産ウェア名を代用
    outputRate: <毎時生産量>,
    inputs: [ { ware: "<材料名>", rate: <毎時消費量> }, ... ]
  },
  ...
}
```
- **全レートは「毎時(per hour)」で統一**。混在させない。
- レシピを持たないウェアは自動的に「採掘/購入材料(RAW)」として扱われる(鉱石・ガス等)。
- `inputs` が空配列のウェアは「原材料なしで生産できるモジュール」(例: エネルギーセルはソーラー発電所のように入力ゼロ)。レシピを持つので RAW ではなく生産モジュール側に入り、`solve`/`buildTree` とも空 `inputs` を正しく無視する(子なしの生産ノードになる)。
- `module` は表示専用のラベル。突き合わせ(需要合算・材料参照)は常に生産ウェア名キーで行うので、ウェア↔モジュールが 1:1 でない将来要件はこの設計では未対応。
- `DB` は SEED そのもの(`const DB = SEED;`)。レシピの追加・修正はコード内 SEED を直接編集する。

### ソルバー `solve(targetWare, targetRate)`
- 再帰的な BOM 展開(部品展開)。目標需要を起点に、各ウェアの必要生産量を全消費元から合算してから上流へ伝播する。共有材料(複数モジュールが食うエネルギーセル等)も正しく合算される。
- DAG 前提だが `onPath` セットで循環を検出し `cycle` を返す(無限ループ防止)。
- 返り値: `{ demand, produced, raw, cycle }`。`produced[]` は `{ ware, module, rate, out, modsExact, mods }`(`module` は表示用ラベル、`modsExact` は実数の必要基数、`mods` は切り上げ整数)、`raw[]` は `{ ware, rate }`。必要生産モジュールテーブルは `module` / `rate` / `out` / `modsExact`(実数)のみ表示する。

### ツリー `buildTree(ware, rate, depth)`
- 「生産カスケード」表示用に、どの材料がどこから消費されるかの明示的な木を構築。深さ上限 12。

### 永続化
- **なし**。レシピは SEED が唯一のソースで、ブラウザ保存(localStorage / `window.storage`)は行わない。SEED を編集してコミット → デプロイすれば全端末に反映される。
- 以前は `x4_recipes_v1` キーの Store シムでブラウザ保存していたが、「保存済みデータが SEED の更新を上書きしてしまう」問題を避けるため撤去した。

### UI 構成
目標設定パネル / サマリーカード / 生産カスケード(木) / 採掘・購入材料テーブル / 必要生産モジュールテーブル。目標は「生産ウェア + モジュール数」を入力し、目標生産量(個/h)は `モジュール数 × outputRate` で算出する(生産量の直接入力・入力モード切替は持たない)。必要生産モジュールテーブルは生産モジュール名・必要量/h・1基あたり/h・必要基数(実数)を表示する。レシピ編集 UI は持たない(SEED をコードで編集する)。

### 見た目
- X4 風のダーク産業コンソール。色とタイポは CSS 変数(`:root`)で定義(`--void` 背景 / `--cyan` 生産系 / `--amber` 採掘・購入系 / `--ember` 警告)。
- 新規 UI を足すときも既存トークンに従い、配色・フォントを増やさない。

## ドメイン注意点(X4 v9.0)

- **レシピ数値は要検証**。断定で埋めず、ゲーム内 Encyclopedia(v9.0 でウェア→生産モジュールが直接リンク)または効率計算機 https://qsna.eu/x4/efficiency と照合する。出典不明な数値はプレースホルダと明示する。
- v9.0 の採掘地域制(mining regionalization)により供給スループットの前提が変わるため、上流チェーンを足す際は注意。
- 初期シードのクレイトロニクスは、元データにあったエネルギーセルの重複行を入力ミスとみなし 1 行(560/h)に統合済み。

## 編集規約(future Claude / contributors へ)

- **単一 HTML の自己完結を崩さない**。外部 JS ライブラリの追加、npm / ビルドステップの導入、ファイル分割はしない。CDN フォント以外の依存を増やさない。
- レシピは SEED 単一ソース。ブラウザ保存(localStorage 等)を再導入しない。レシピの追加・修正は SEED を直接編集する。
- レートは毎時で統一。新フィールドを足すときも既存の per-hour 前提と整合させる。
- レシピの数値を出典なしに断定しない。
- ソルバーのロジックは純粋関数(`solve` / `buildTree`)なので、検証したい場合は当該関数だけ Node に切り出して単体テスト可能(リポジトリにテストハーネスは未同梱)。

## 今後の候補(任意)

- レシピ DB の JSON エクスポート/インポート(SEED 編集なしでレシピを差し替える手段。現状は SEED をコード編集する運用)。
- 電力収支・必要面積・建設コストの概算表示。
- 在庫推移など時間挙動を扱う場合は、本ツールは「planner」なので別機能として明確に分離する。